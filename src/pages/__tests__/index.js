import React from "react"
//import { render } from "@testing-library/react"
import { useStaticQuery } from "gatsby"

import BlogIndex from "../index"

describe("Index component", () => {
  beforeEach(() => {})
  test("equalty matchers", () => {
    expect(2 * 2).toBe(4)
  })
})
